# Mobile Agent

Owns Expo React Native, offline-first sync, SQLite storage, camera/GPS capture, and secure local storage.

## Standards

- Offline queue before network submission.
- Explicit sync status for every local record.
- Conflict-safe IDs and timestamp metadata.
- Local sensitive data must be encrypted where supported.

